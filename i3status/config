# i3status configuration file.
# see "man i3status" for documentation.

# It is important that this file is edited as UTF-8.
# The following line should contain a sharp s:
# ß
# If the above line is not correctly displayed, fix your editor first!

general {
        colors = true
	# let's add some better colors
	color_good = "#f6f1a2"
	color_bad = "#f1a2f6"
        interval = 5
}

order += "cpu_temperature 0"
order += "cpu_usage"
#order += "wireless _first_"
#order += "ethernet _first_"
order += "volume master"
order += "battery 0"
order += "tztime local"

cpu_temperature 0 {
        format = "T: %degrees°C"
        path =  "/sys/class/hwmon/hwmon0/temp1_input"
        max_threshold = "80"
}

cpu_usage {
        format = "CPU: %usage"
}

wireless _first_ {
        format_up = "W: %quality @ %essid %ip"
        format_down = "W: down"
}

ethernet _first_ {
        # if you use %speed, i3status requires root privileges
        format_up = "E: %ip (%speed)"
        format_down = "E: down"
}

# Volume display, had to use the name "volume master"

volume master {
        format = "♪:%volume"
        device = "default"
        mixer = "Master"
        mixer_idx = 0
}

# Added better battery
battery 0 {
        format = "%status %percentage %remaining"
        format_down = "No battery"
        status_chr = "⚇ Chr"
        status_bat = "⚡ Bat"
        status_full = "☻ Full"
        path = "/sys/class/power_supply/BAT1/uevent"
        low_threshold = "40"
        threshold_type = "time"
        integer_battery_capacity = true
        hide_seconds = true
}

run_watch DHCP {
        pidfile = "/var/run/dhclient*.pid"
}
# Changed the time setting to a bit nicer
tztime local {
        format = "%d-%m-%Y %H:%M"
}
